label: Related products
type: builder
fieldsets:
  product:
    label: Related product
    entry: >
      {{product}}
    fields:
      product:
        type: select
        options: query
        query: 
          page: shop
          fetch: pages
          template: product